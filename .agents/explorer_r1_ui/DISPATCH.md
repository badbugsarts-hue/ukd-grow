# Task Dispatch — Explorer UI

## Mission

Survey existing UI components, styles, navigation, and modal structures in `src/App.tsx`, `src/components/`, `src/styles.css`, and `.antigravitz/`.
Determine layout and UX patterns for:

1. Equipment Manager & 9-point PPFD Mapping Modal / Unterseite.
2. Sensor Calibration Manager UI (pH/EC calibration state & history).
3. Plant Identity Modal & Day Zero Time Anchor UI.
4. Pot Weight Tracking UI & interactive controls.

## Constraints & Requirements

- Read ORIGINAL_REQUEST.md and AGENTS.md.
- Ensure 2026 Master Class Elite design using existing CSS variables (`var(--green)`, `var(--surface-1)`, etc.).
- Ensure German terminology with tooltips (`TermTooltip.tsx`) and accessible touch targets (44px min).
- Produce a detailed handoff report in your working directory (`handoff.md`).
